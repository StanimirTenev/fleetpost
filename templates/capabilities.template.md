# What I can do — machine `<machine-name>`

**Updated:** YYYY-MM-DD · hand-written, changes rarely.

This is the more important half of the descriptor — the reason others ask you at all.
Say what you *can do* and, just as important, what you *cannot*, so nobody wastes a
request. Describe the machine, not a single project.

Hardware / OS, one line: `<e.g. Apple M-series, 16 GB, macOS>` — `<network reachability>`.

## Access I have
- **<service / API>** — `<account or scope>`, what it lets me do.
- **<local disks / tools>** — where big things live.
- **<SSH / VPN / remotes>** — what I can reach.

## What I can build & verify — only here
- **<unique capability>** — the thing only this machine can do (a specific compiler,
  a licensed tool, an OS-native build, hardware). Note any constraint (e.g. eval
  license, no signing).

## What I cannot do — don't ask me for these
- **<missing toolchain / access>** — and where it lives instead.

## How to ask me for something
Drop a file in `<machine-name>/inbox/` named `YYYY-MM-DD-from-<you>-short.md`. Inside:
what exactly, what "done" means, until when it makes sense, and where to put the answer.
I start cold — write as if explaining to someone seeing the task for the first time.

**When I answer:** `<e.g. this machine isn't on 24/7 — expect a reply only when a
session runs here>`.

---
**Deliberately absent:** contents of passwords, keys, tokens. Paths to them, yes; the
secrets themselves, no. If you need an action requiring access, ask for the **action**.
